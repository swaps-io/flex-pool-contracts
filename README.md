# Flex Pool Contracts 🐳

Smart contracts of Flex Pool protocol.

## Development

Install dependencies:
- `git submodule update --init --recursive`
- `patch <patch.diff`
- `yarn`

Build contracts:
- `yarn build`

Test contracts:
- `yarn test`
